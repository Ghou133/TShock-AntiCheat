# 安全与问题报告

## 支持边界

目前只有开发源码，没有获得完整生产验收的发行版本。现有运行校验与规则资格只适用于各自明确锁定的环境，不应将源码中的 `Production` 枚举视为安全承诺。部署限制见 [构建指南](docs/BUILDING.md) 和 [已知问题](docs/REVIEW-2026-09.md)。

## 报告内容

提供源码提交、运行包版本与哈希、启用模式、相关规则编号、预期与实际结果，以及使用自有隔离环境的最小复现。移除账号凭证、令牌、IP、角色私密信息和真实数据库；必要时用合成账号重现。

普通构建或文档错误可提交 Issue。对于仍可用于破坏服务器的具体漏洞，优先使用仓库启用的 GitHub 私密漏洞报告入口；若该入口未启用，先提交不含利用细节的联系请求，请维护者指定私密渠道。本项目不虚构未配置的安全邮箱，也不承诺固定响应时限。

## 运行与证据

在独立副本中测试，保留备份及版本证据。不要在真实服务器上运行破坏性用例。观察日志不是处罚事实；不要把日志写盘故障、单次权限拒绝或资源超额解释为作弊。

不要为了恢复连接而直接删除处罚日志或 `run-safety.state`。异常退出后的确认恢复需要核对已有处罚与可能未落盘的窗口；当前缺少完整的公开运维工具，这也是发布阻塞项。
